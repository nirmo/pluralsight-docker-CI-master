# The deployment section should be excluded until
# it is explicitly included in module 5

deployment:
  dockerhub:
    branch: master
    commands:
      - "curl -H "Content-Type:application/json" --data '{"build":true}' -X POST https://registry.hub.docker.com/u/nirm/pluralsight-docker-ci-master/trigger/b57aa708-aed6-40f6-977e-"

test:
  override:    
    - mocha